kind: pipeline
type: docker
name: deploy notodo

clone:
  retries: 3

volumes:
  - name: dockersock
    host:
      path: /var/run/docker.sock

steps:
  - name: test
    image: golang:1.20
    commands:
      - export GOPROXY=https://goproxy.cn
      - go test -v ./...

  - name: deploy
    image: docker:dind
    volumes:
      - name: dockersock
        path: /var/run/docker.sock
    commands:
      - export GOPROXY=https://goproxy.cn
      - docker compose up -d
    environment:
      MONGO_URI:
        from_secret: MONGO_URI
